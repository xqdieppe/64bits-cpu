#include <stdio.h>

#include "gates.h"
#include "registers.h"
#include "memory.h"

int main(int argc, char **argv) {
	//GATE_TEST_FULL(NAND);
	//GATE_TEST_FULL(NOR);
	//REG(rip);
	ONE_SHORT_MEMORY(0);
}
